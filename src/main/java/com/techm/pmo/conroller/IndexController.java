package com.techm.pmo.conroller;

import javax.servlet.http.HttpServletRequest;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
public class IndexController {

  /*
   * @RequestMapping("/") String index() { return "login"; }
   * 
   * @RequestMapping("/pm_index") String pmindex(HttpServletRequest request,Model model) {
   * 
   * model.addAttribute("user", request.getSession().getAttribute("userDtl")); return
   * "pmo/pm_index"; }
   */

}
